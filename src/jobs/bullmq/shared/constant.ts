export class JobConstant {
  static readonly CRAWL_COMIC_JOB_NAME = '[CrawlEngine]:=(crawl-comic)';
  static readonly CRAWL_CHAPTER_JOB_NAME = '[CrawlEngine]:=(crawl-chapter)';
  static readonly UPDATE_COMIC_JOB_NAME = '[CrawlEngine]:=(update-comic)';
  static readonly UPLOAD_JOB_NAME = '[CrawlEngine]:=(upload-image-to-drive)';
}
